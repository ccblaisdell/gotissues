$('#issue_<%= @issue.id %>')
  .removeClass('<%= Issue.states.join(" ") %>')
  .addClass('<%= @issue.status %>')
  .find('.status')
    .html('<%= @issue.status %>')
    .removeClass()
    .addClass('status <%= @issue.status %>');